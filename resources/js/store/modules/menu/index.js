import dashboard from './dashboard'
import inspections from './inspections'

const state = {
  items: [
    dashboard,
    inspections,
  ],
}

const mutations = {}

const actions = {}

export default {
  state,
  mutations,
  actions,
}
